import { Layout } from 'antd'
import React from 'react'
import './header.less'

const { Header } = Layout

const CommonHeader = () => {
    return (
        <Header className="header">
            Header
        </Header>
    )
}

export default CommonHeader
